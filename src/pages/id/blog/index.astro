---
import { getCollection } from 'astro:content';
import BlogList from '../../../components/BlogList.astro';

const posts = await getCollection('blog', ({ data }) => data.lang === 'id');
---

<BlogList locale="id" posts={posts} />
